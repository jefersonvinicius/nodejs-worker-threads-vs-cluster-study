{
  "name": "nodejs-worker-vs-cluster",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "yarn tsc",
    "thread": "yarn build && node build/thread.js",
    "cluster": "yarn build && node build/cluster.js"
  },
  "dependencies": {
    "@types/node": "^17.0.8",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.4"
  }
}
